查找文章中各个单词出现的频率
思路： 
1，创建一个变量来存储共出现了多少个单词，可以使用正则表达式来确定是否是一个单词，单词的结尾是空格键、逗号、句号。
2，如果使用的Python语言，可以直接将每个单词存储在列表中，然后再查找单词的时候，遍历列表。如果不存在，就将该单词存在列表中
3，再创建第二个列表，用来存储每个单词出现的次数，和第一个列表中的单词相对应。
4，这是用编程的方法解决的问题，数据结构中使用的是逐渐匹配，是最基础的代码。
